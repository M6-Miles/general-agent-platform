apiVersion: 1
providers:
  - name: Agent Runtime
    orgId: 1
    folder: Agent Runtime
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
